instructions to setup project
first install node 
link - https://nodejs.org/en

open a folder in cmd
type cmd in address bar and press enter

run command to create vite project- npm create vite@latest
cd projectName
npm install

now open that project folder in vs code ( code . )


when we download or clone an node/react project from git then we run "npm -install"


for open or host the project use this command to 'npm run build'

 